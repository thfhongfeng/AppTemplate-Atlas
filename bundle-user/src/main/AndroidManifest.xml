<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pine.user">

    <application
        android:name=".app.UserApplication"
        android:allowBackup="true"
        android:supportsRtl="true">
        <activity
            android:name=".ui.activity.UserHomeActivity"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan|stateHidden"></activity>

        <!-- atlas remote router begin -->
        <meta-data
            android:name="atlas.transaction.intent.action.user.UserBundleRemoteAction"
            android:value="com.pine.user.remote.atlas.UserBundleRemote" />
        <!-- atlas remote router end -->
    </application>
</manifest>